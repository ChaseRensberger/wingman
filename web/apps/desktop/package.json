{
  "name": "@wingman/desktop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "bun run generate-assets && deno desktop --no-npm --allow-net=127.0.0.1 --config deno.json main.ts",
    "generate-assets": "bun run scripts/generate-assets.ts"
  }
}
