# autoregprov (Auto Register Providers)

This exists only to auto register the providers so you can easily access them when you start the server. In the future I'll probably lazy load them or come up with a better way to do something similar.
