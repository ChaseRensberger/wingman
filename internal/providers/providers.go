// Package providers registers all built-in providers.
// Import this package for side effects to register all providers with the registry.
package providers

import (
	_ "wingman/provider/anthropic"
)
