// Package hook is a sibling to wingagent/plugin: it ships small,
// single-seam hook implementations that don't warrant the full plugin
// bundle ceremony. v0.1 ships no built-ins (compaction moved to
// wingagent/plugin/compaction, which is the canonical multi-seam
// example). Reserved for future single-purpose helpers.
package hook
